programa {
  funcao inicio() {
    real a, b, at 

    escreva("qual é a altura: ")
    leia(a)

    escreva("qual é a base: ")
    leia(b)

    at = (a*b) / 2

    escreva("O valor da área é:", at)
  }
}
