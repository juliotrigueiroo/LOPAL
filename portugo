programa {
  funcao inicio() {
   inteiro maior = 0, indicie 
   inteiro numero, i, numero[8]

   para(i = 0; < 8, i++){
    escreva("qual o valor do indicie ",i,": ?\n")
    leia(n[i])
   } 

   para(i = 0; i < 8; i++){
    se(n[i] > maior){
      maior = n(i)
      indicie= i 
    }
   }
  }
  escreva("O maior valor é:",maior)
  esreva("\no maior valor está armazenado no índicie", indicie)
}
